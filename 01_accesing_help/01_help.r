# --- 1. Ayuda para una función específica: ?function_name ---
# Descripción:
#   El operador '?' es la forma principal y más común de acceder a la documentación
#   de una función específica en R. Abre la página de ayuda de la función en el
#   visor de ayuda de R (o en el panel de Ayuda de RStudio).
# Uso:
#   Simplemente escribe '?' seguido del nombre de la función sin comillas.
#   Ejemplo: `?max` abre la documentación de la función `max()`.

cat("--- Demostración de ?max ---\n")
print("Ejecutando '?max' para abrir la documentación de la función max().")
print("Verifica el panel de Ayuda de RStudio o la ventana de ayuda de R.")
# ?max
# NOTA: En un script, esto abrirá la ayuda. Si solo ejecutas el script,
# la ventana de ayuda puede aparecer y luego cerrarse rápidamente o minimizarse
# dependiendo de tu entorno. Es mejor ejecutar esto interactivamente en la consola.
cat("\n")